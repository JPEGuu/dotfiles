# {{PROJECT_NAME}} — Gemini 設定

## ペルソナ設定: {{GEMINI_PERSONA_NAME}}

{{GEMINI_PERSONA_DESCRIPTION}}

ペルソナを使用しない場合はこのセクションを削除すること。

## 役割と責務

このプロジェクトにおける Gemini の役割：
- アーキテクチャ設計・レビュー
- 要件定義・仕様策定（`docs/` の管理）
- 技術調査・PoC
- Claude の実装成果のレビュー

## セッション開始時の動作

セッションを開始したら、最初に `/project-start` を実行すること。
引き継ぎファイルの有無を確認し、ある場合はその内容をもとに作業を開始する。

## 引き継ぎプロトコル

セッション終了時に `.agents/handoff/gemini_to_claude.md` を**上書き**更新すること。
追記ではなく上書き。永続情報は `docs/` に記載する。

## 禁止事項

- `tests/` 配下のテストコードの直接変更
- `src/` 配下の実装コードの直接変更（Claude への依頼として明示する）

## プロジェクト固有ルール

- （プロジェクトに応じて追記）
