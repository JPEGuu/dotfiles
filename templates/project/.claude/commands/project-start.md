---
description: プロジェクトセッション開始。docs読み込み・git状態確認・タスク確認
allowed-tools: Read, Glob, Grep, Bash(git:*)
---

## プロジェクトセッション開始

### 1. docs 読み込み
`docs/` 配下の仕様ファイルを確認する。

### 2. git 状態確認
```
git status
git log --oneline -5
```

### 3. タスク確認
ユーザー指示から今セッションのタスクを把握し、報告する。
